# Django Hello World on Embr

Minimal Django app to validate Embr's Python/Django build & deploy pipeline.

- Health endpoint: `/health`
- Index: `/`
- `ALLOWED_HOSTS = ['*']` so sandbox hostnames are accepted.
